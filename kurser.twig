{% extends 'partials/_master.twig' %}

{% block main %}

<div class="bg-striped">
	<section class="container">
		<h2 class="content-divider">Våra kurser</h2>
		{% for kurs in records %}
			{% include 'partials/_kurs-front.twig' %}
		{% endfor %}
	</section>
</div>

{% endblock main %}


    <div class="container-gray">
    	<section class="container">
		  {% setcontent kurserRubrik = 'block/kurser-rubrik' %}
		  <h1 class="text-center">{{ kurserRubrik.rubrik }}</h1>
		  <div class="row">
			{% setcontent kurser = 'kurser' %}
			{% for kurs in kurser %}
			<div class="col-sm-6 col-md-4 kurs-front">
			 <a href="kurs/{{ kurs.slug }}">
			  <div class="image-div">
				  <img src="{{ kurs.bild|image(400, 175) }}" class="img-responsive">
				  <h2>{{ kurs.namn }}</h2>
			  </div>
			 </a>
			<div class="bg-white">
			 <div class="smaller-text">
			  {{ kurs.teaser }}
			 </div>
			 <a href="kurs/{{ kurs.slug }}" class="btn btn-info btn-block">LÄS MER</a>
			</div>
			</div>
			{% endfor %}
		  </div>
		</section>
    </div>
